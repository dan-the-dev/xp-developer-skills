# Anti-patterns

- Two increments in one PR without agreement
- Whole-feature acceptance file while doing one slice
- Skip ATDD or skip TDD for a user-visible slice
- Forget to mark parent `increments/…` line `[x]`
